// Entry point for the future Vue 3 + TypeScript frontend.
// Currently a placeholder — the live app is in public/js/app.js.
// New modules will be added here incrementally and loaded from public/index.html via /dist/main.js.

import { createApp } from 'vue';

// Placeholder — remove when first real component is added
const app = createApp({ template: '<div></div>' });
app.mount('#vue-root');
