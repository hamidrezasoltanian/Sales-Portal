# برنامه رفع ایرادها و بهبود امنیت — Click CRM

> سند برنامه‌ریزی برای رفع ایرادهای جدی شناسایی‌شده در ممیزی کد.
> هر مورد با محل دقیق در کد (`file:line`)، اثر، راه‌حل، ریسک و معیار پذیرش ثبت شده است.
> این سند مکمل `DB_SECURITY_TODO.md` (اقدامات زیرساختی) است و روی ایرادهای **کد** تمرکز دارد.

## راهنمای شدت

| نماد | معنی |
|---|---|
| 🔴 بحرانی | نشت داده/دسترسی غیرمجاز؛ باید فوری رفع شود |
| 🟠 مهم | ریسک یکپارچگی داده یا امنیت جدی؛ در اولویت بعدی |
| 🟡 قابل توجه | بدهی فنی/بهبود کیفیت؛ زمان‌بندی‌شده |

## راهنمای وضعیت

`⬜ باز` · `🔄 در حال انجام` · `✅ انجام‌شده` · `⏸ معلق`

---

## 🔴 بحرانی

### C-1 — نشت کردنشیال واقعی MS-SQL در گیت
- **وضعیت:** ✅ انجام‌شده (کد؛ rotate عملیاتی هنوز لازم)
- **مدرک:** `server/sync_service.js:10-14` (`SQL_HOST='192.168.4.4'`, `SQL_USER='ma'`, `SQL_PASS='adminS@2'`, `SQL_DB='faradissoftatenazist'`)؛ همچنین `scripts/inspect_factor_columns.js:4`, `scripts/inspect_factor_items.js:4`, `scripts/inspect_mssql.js:4`, و `scripts/migrate_sync_schema.js:14` (`password:'62604193'`).
- **اثر:** رمز دیتابیس حسابداری در تاریخچه‌ی گیت قابل استخراج است. `sync_service.js` حتی جایی import نمی‌شود (کد مرده با راز لو‌رفته).
- **راه‌حل:**
  1. **چرخاندن (rotate) فوری** رمز کاربر `ma` روی SQL Server (اقدام خارج از مخزن).
  2. انتقال همه‌ی مقادیر به env (`FARADIS_SERVER/FARADIS_USER/FARADIS_PASSWORD/...`) مطابق `server/integrations/faradis.js` که از قبل از env می‌خواند.
  3. حذف hardcodeها از `sync_service.js` و اسکریپت‌ها؛ اگر `sync_service.js` استفاده نمی‌شود، حذفش.
  4. پاک‌سازی راز از history گیت (`git filter-repo` یا BFG) + ابطال هر توکن مرتبط.
- **ریسک تغییر:** پایین در کد؛ اما rotate باید هماهنگ با تیم عملیات انجام شود.
- **معیار پذیرش:** هیچ رشته‌ی رمز/IP در کد با `git grep` پیدا نشود؛ اتصال فرادیس فقط از env بخواند؛ `sync_service` بدون راز اجرا شود یا حذف شده باشد.

### C-2 — مجوزدهی (RBAC) فقط سمت کلاینت روی API اصلی داده
- **وضعیت:** ✅ انجام‌شده (فیلتر GET/PUT در `server/lib/center-ownership.js`)
- **مدرک:** `server/routes/data.js:134` (`GET /db` کل DB را برمی‌گرداند)، `server/routes/data.js:151` (`PUT /db` کل DB را بازنویسی می‌کند)، `loadDBFromSQL` (خط ۱۶) بدون فیلتر `req.user`. طبق `CLAUDE.md` محدودیت «کارشناس فقط مراکز خودش» فقط در مرورگر اعمال می‌شود.
- **اثر:** هر کاربر لاگین‌شده با یک درخواست ساده می‌تواند **کل داده‌ی تیم** را بخواند و بازنویسی/خراب کند.
- **راه‌حل (تدریجی برای کاهش ریسک):**
  1. لایه‌ی مجوز سمت سرور: در `GET /db` خروجی را بر اساس مالکیت مرکز (زنجیره‌ی canonical owner) برای نقش `کارشناس فروش` فیلتر کن؛ مدیران کل را ببینند.
  2. در `PUT /db` نوشتن را به رکوردهای متعلق به کاربر محدود کن (merge سمت سرور به‌جای overwrite کامل).
  3. میان‌مدت: مهاجرت از تک‌بلاب به endpointهای per-collection (به C-4 گره خورده).
- **ریسک تغییر:** متوسط–بالا (منطق مالکیت پیچیده و در چند جا کپی شده). نیاز به تست رگرسیون قوی.
- **معیار پذیرش:** کاربر کارشناس از طریق API فقط مراکز خودش را ببیند/تغییر دهد؛ تلاش برای دسترسی به مرکز دیگری → داده‌ی فیلترشده یا 403؛ تست خودکار جدید در `tests/`.

### C-3 — گیت پسورد قیمت‌گذاری در جاوااسکریپت کلاینت
- **وضعیت:** ✅ انجام‌شده (`/api/pricing/mgmt/verify` + `/api/pricing/costs`)
- **مدرک:** `public/js/app.bundle.js:4608`, `public/js/app.bundle.js:4837`, `public/js/pricing.js:133` (`'62604193'`)؛ همان مقدار به‌عنوان رمز DB در `scripts/migrate_sync_schema.js:14`.
- **اثر:** رمز حاشیه‌ی سود/تنظیمات قیمت با View Source قابل مشاهده است؛ محافظت واقعی نیست + بازاستفاده‌ی راز.
- **راه‌حل:**
  1. انتقال بررسی رمز به سمت سرور (endpoint که رمز را با hash در env/DB مقایسه کند و یک مجوز موقت بدهد).
  2. گیت‌کردن دادهٔ حساس (حاشیه سود) پشت همان مجوز سمت سرور، نه صرفاً پنهان‌کردن UI.
  3. حذف مقدار hardcode از کلاینت و از اسکریپت مهاجرت.
- **ریسک تغییر:** متوسط (نیاز به ویرایش `app.bundle.js` با احتیاط NBSP طبق `CLAUDE.md`).
- **معیار پذیرش:** رمز در هیچ فایل کلاینت نباشد؛ دادهٔ حاشیه فقط با مجوز سمت سرور برگردد.

---

## 🟠 مهم

### H-1 — مدل تک‌بلاب با last-write-wins (ریسک از دست رفتن داده)
- **وضعیت:** ✅ انجام‌شده (مرحله ۱: `PATCH /api/data/patch` + merge یادداشت‌ها + `savePatchDB` در `setE`)
- **مدرک:** `server/routes/data.js:151-190` (PUT کل DB + تشخیص conflict شکننده)؛ اعتراف‌شده در `CLAUDE.md` (بخش Roadmap → Data layer evolution).
- **اثر:** تحت همزمانی چند کاربر، بازنویسی داده‌ی واقعی ممکن است.
- **راه‌حل:** مهاجرت تدریجی به endpointهای per-collection (tasks/notifications/changeLog از قبل SQL هستند)، merge خوش‌بینانه به‌جای overwrite، استفاده از کانال SSE موجود (`/api/events`) برای refresh زنده.
- **ریسک تغییر:** بالا (تغییر معماری). به‌صورت مرحله‌ای انجام شود.
- **معیار پذیرش:** ذخیره‌ی هم‌زمان دو کاربر روی دو مرکز مختلف داده‌ی یکدیگر را پاک نکند؛ تست همزمانی سبز.

### H-2 — مدیریت ضعیف توکن (ابطال/کوکی/رمز پیش‌فرض)
- **وضعیت:** ✅ انجام‌شده (`token_version` + secure cookie + fail-fast prod)
- **مدرک:** `server/routes/auth.js:160` (logout فقط کوکی را پاک می‌کند)، `server/routes/auth.js:11-15` (کوکی بدون `secure`)، `server/auth.js:6-12` (fallback رمز پیش‌فرض فقط warning).
- **اثر:** توکن دزدیده‌شده تا ۷ روز معتبر می‌ماند؛ روی HTTP کوکی محافظت‌نشده؛ اجرای ناامن با رمز پیش‌فرض ممکن است.
- **راه‌حل:**
  1. اضافه‌کردن `secure:true` به کوکی در production.
  2. مکانیزم ابطال (لیست سیاه توکن یا `token_version` در `app_users` که در `requireAuth` چک شود).
  3. در production اگر `JWT_SECRET` پیش‌فرض بود، اجرا متوقف شود (fail-fast) نه فقط warning.
- **ریسک تغییر:** پایین–متوسط.
- **معیار پذیرش:** logout توکن را باطل کند؛ کوکی در prod `secure` باشد؛ استارت با رمز پیش‌فرض در prod شکست بخورد.

### H-3 — `requireAuth` هنگام خطای DB باز می‌شود (fail-open)
- **وضعیت:** ✅ انجام‌شده (fail-closed → 503)
- **مدرک:** `server/auth.js:71-74` (در catch، درخواست عبور داده می‌شود).
- **اثر:** هنگام اختلال DB، کاربر غیرفعال‌شده ممکن است دسترسی داشته باشد.
- **راه‌حل:** رفتار fail-closed (بازگشت 503/401) یا حداقل عبور فقط با JWT معتبر و بدون permissions؛ لاگ ساختارمند.
- **ریسک تغییر:** پایین (اما باید مطمئن شد اختلال گذرای DB کل سرویس را قفل نکند — timeout/retry).
- **معیار پذیرش:** با DB down، درخواست‌های محافظت‌شده رد شوند؛ health همچنان 503 بدهد.

---

## 🟡 قابل توجه

### N-1 — لاگ دیباگ حساس روی هر ذخیره
- **وضعیت:** ✅ انجام‌شده (پشت `DEBUG_CONFLICT=1`)
- **مدرک:** `server/routes/data.js:185` (`console.log('[DEBUG CONFLICT]', ...)`).
- **راه‌حل:** حذف یا پشت فلگ `DEBUG` بردن.
- **معیار پذیرش:** در لاگ عادی چاپ نشود.

### N-2 — CSP غیرفعال در helmet
- **وضعیت:** ✅ انجام‌شده
- **مدرک:** `server/index.js:29` (`contentSecurityPolicy:false`).
- **راه‌حل:** تعریف CSP سازگار با CDNهای استفاده‌شده (Vazirmatn، xlsx) به‌جای خاموش‌کردن کامل.
- **معیار پذیرش:** CSP فعال باشد و اپ بدون خطای کنسول لود شود.

### N-3 — سطح حمله XSS در فرانت
- **وضعیت:** ✅ انجام‌شده (مرحله ۱: `safe-dom.js`, esc در modal/contact popup, `stripScripts`)
- **مدرک:** الگوی سراسری ساخت HTML با الحاق رشته و `innerHTML` که به `esc()` دستی تکیه دارد (`public/js/*`).
- **راه‌حل:** ممیزی نقاط `innerHTML`، اطمینان از `esc()` روی همه‌ی ورودی‌های کاربر؛ میان‌مدت مهاجرت به Vue (اسکلت `src/`).
- **معیار پذیرش:** تزریق `<script>`/`<img onerror>` در فیلدهای متنی اجرا نشود.

### N-4 — بهداشت مخزن
- **وضعیت:** ✅ انجام‌شده (`sync_hashes.json` حذف + `sync_service.js` حذف)
- **مدرک:** `sync_hashes.json` (۸.۴MB) tracked در گیت؛ `server/sync_service.js` کد مرده.
- **راه‌حل:** افزودن `sync_hashes.json` به `.gitignore` و حذف از ردیابی؛ حذف/آرشیو کد مرده.
- **معیار پذیرش:** فایل‌های حجیم/مرده از مخزن حذف شوند.

### N-5 — نبود فریم‌ورک مهاجرت اسکیمای DB
- **وضعیت:** ✅ انجام‌شده (`server/migrations/runner.js` + فایل‌های SQL ورژن‌دار)
- **مدرک:** `server/db.js` اسکیمای را با `CREATE/ALTER ... IF NOT EXISTS` هنگام بوت می‌سازد؛ بدون ورژن/rollback.
- **راه‌حل:** افزودن ابزار migration سبک (مثل node-pg-migrate) به‌صورت تدریجی.
- **معیار پذیرش:** تغییرات اسکیمای ورژن‌دار و قابل rollback.

### N-6 — مونولیت فرانت‌اند
- **وضعیت:** 🔄 در حال انجام (`SyncStatusBadge.vue` + پنل‌های Vue موجود)
- **مدرک:** `public/js/app.bundle.js` (~۱۵هزار خط، دارای NBSP).
- **راه‌حل:** مهاجرت تدریجی به کامپوننت‌های Vue در `src/` (اسکلت موجود).
- **معیار پذیرش:** ماژول‌های جدید به‌جای بلاب در `src/` نوشته شوند.

### N-7 — dev و prod روی یک دیتابیس
- **وضعیت:** ✅ انجام‌شده (`scripts/setup_dev_db.sh` + `dev-guard` هشدار استارت)
- **مدرک:** `CLAUDE.md` (بخش Dev vs Prod environment).
- **راه‌حل:** DB جدا برای dev (`atena_crm_dev`) مطابق دستورالعمل موجود در `CLAUDE.md`.
- **معیار پذیرش:** تست بک‌اند در dev به داده prod دست نزند.

---

## ترتیب پیشنهادی اجرا

| گام | موارد | چرا اول |
|---|---|---|
| ۱ | C-1 (rotate + env)، N-1، N-4 | کم‌ریسک، اثر امنیتی فوری، پاک‌سازی راز |
| ۲ | H-2، H-3، N-2 | سخت‌کردن احراز هویت و هدرها؛ تغییرات موضعی |
| ۳ | C-3 | حذف گیت پسورد کلاینت و انتقال به سرور |
| ۴ | C-2 | RBAC سمت سرور (نیاز به تست رگرسیون قوی) |
| ۵ | H-1، N-5، N-6، N-7 | تغییرات معماری بلندمدت و تدریجی |

## اصول اجرا (برای هر تغییر)
- طبق `CLAUDE.md`: ویرایش `app.bundle.js` فقط با اسکریپت (خطر NBSP) و سپس `node --check public/js/app.bundle.js`.
- هر تغییر: برنچ جدا، commit توصیفی، اجرای `npm test` (۲۹ تست فعلی سبز بماند) و افزودن تست برای رفتار جدید.
- تغییرات بک‌اند فقط روی DB جدا تست شوند تا داده prod آسیب نبیند (N-7).

## چک‌لیست پیگیری
- [x] C-1 نشت کردنشیال MS-SQL
- [x] C-2 RBAC سمت سرور روی `/api/data/db`
- [x] C-3 حذف گیت پسورد قیمت‌گذاری از کلاینت
- [x] H-1 مهاجرت از تک‌بلاب / merge خوش‌بینانه (مرحله ۱)
- [x] H-2 مدیریت توکن (ابطال + secure + fail-fast رمز)
- [x] H-3 رفتار fail-closed در `requireAuth`
- [x] N-1 حذف لاگ دیباگ
- [x] N-2 فعال‌کردن CSP
- [x] N-3 ممیزی XSS (مرحله ۱)
- [x] N-4 بهداشت مخزن (sync_hashes.json / کد مرده)
- [x] N-5 فریم‌ورک مهاجرت DB
- [ ] N-6 مهاجرت تدریجی فرانت به Vue (ادامه‌دار)
- [x] N-7 جداسازی DB dev/prod
